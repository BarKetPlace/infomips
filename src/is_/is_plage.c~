#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#include "is_.h"



int is_hexa(char* chaine) {
	
    int i=2;
	if (chaine!=NULL && strlen(chaine)>2&& chaine[0]=='0' && chaine[1]=='x' && sscanf(chaine,"%x",&i)==1)
	{		
		for (i=2; i<strlen(chaine);i++)
		{ 	
			if ( isxdigit(chaine[i]) == 0 ) return 0;
		}
		return 1;
	}
	return 0;
}

int is_plage(char * chaine)
{
	int i=0; 
	char* r;
	char* g;

	if (is_hexa(r)==1) 
	{
		g=strtok(NULL, ":");puts(g);
		if (is_hexa(g)==1) {return 1; }
		else{return 0;}
}
	else{return 0;}

}

/*
int main(int argc, char* argv[])
{	printf("ok\n");
	
	printf("%d\n",is_hexa(argv[1])); return 0;

	int m=is_plage(argv[1]);
	printf("%d\n",m);
}
*/

	/*while (chaine[i] != ':')
		{ 
			r[i]=chaine[i];
			i++;
		}
	r[i+1]='\0';
	if (is_hexa(r)==1)
		{
			i=i+1;
			char g[1024];

				while (chaine[i] != '\0') 
			{
				g[i]=chaine[i];
				i++;
			}

				g[i+1]='\0';
				if (is_hexa(g)==1) {return 1;}
		}

	else {return 0;}*/
