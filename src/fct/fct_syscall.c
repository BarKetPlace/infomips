#include <strings.h>
#include <string.h>

#include "mem.h"
#include "reg.h"
#include "bits.h"
#include "notify.h"
#include "emulateur.h"

// Fonction SYSCALL


int fct_syscall(inst i, registre* regs)
{
	switch(regs[v0].val)
		case 1: 
			regs[]=affiche_entier();
		case 4:
			regs[]
		case 5:

		case 8:
		
		case 10:
	//int off=((short)inst.i.imm)<<2;
	//if (regs[inst.i.rs]>=0)
		
}
